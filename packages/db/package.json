{
  "name": "@chatbot/db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "deploy:test": "cross-env MODE=test prisma migrate deploy",
    "deploy:production": "cross-env MODE=production prisma migrate deploy",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "catalog:nestjs"
  },
  "devDependencies": {
    "prisma": "catalog:nestjs"
  }
}
